apiVersion: v1
kind: Service
metadata:
  name: {{ include "oriyan-portfolio.fullname" . }}-svc
  labels:
    {{- toYaml .Values.labels | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- toYaml .Values.labels | nindent 4 }}


